import { Inter } from 'next/font/google';
import { Sarabun } from 'next/font/google';
export const inter = Inter({ subsets: ['latin'] })
export const sarabun = Sarabun({ weight: ['400', '800'], subsets: ['latin'] })
